import '../Contact/Contact.css';

function Contact() {
       return (
        <section id="contact" className="contact">
            <h1>CONTACT PAGE</h1>
        </section>
    );
}

export default Contact;
